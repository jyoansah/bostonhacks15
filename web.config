<rewrite>
    <rules>
        <rule name="RewriteAPI" stopProcessing="true">
            <match url="^api/(.+)$" ignoreCase="false" />
            <action type="Rewrite" url="api/api.php?action={C:2}" appendQueryString="false" />
        </rule>
    </rules>
</rewrite>